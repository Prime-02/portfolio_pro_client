Task: Build the Testimonials section of the portfolio, following the same architectural patterns, conventions, and feature set used in the Projects section (core files provided). The Testimonials store file is also provided as a starting point.

Requirements:

1. Card UI Styles & Configurations
   - Testimonials cards must support multiple distinct style variants and configuration options, ensuring the portfolio showcases a mix of design flavors rather than uniform cards.

2. Field-Level Toggle
   - Every individual piece of information within an Testimonials entry must be independently toggleable (show/hide).

3. Layout Types
   - Provide layout types specifically designed for displaying Testimonials in a professional standard.
   - These layouts must be visually and structurally distinct from the layouts used in the Projects section.

4. Public Filters
   - Implement filter controls for public users using the following TestimonialsFilters interface exactly:
     export interface PublicTestimonialsByUsernameFilters {
     username: string;
     skip?: number;
     limit?: number;
     is_featured?: boolean;
     author_company?: string;
     author_relationship?: string;
     rating?: number;
     ids?: string[];
     merge_filters?: boolean;
     }

5. Card Override Support
   - Testimonials must continue to support card-level overrides, following the simillar override pattern used in the Projects section.

6. Animations, Background & CTA
   - Implement Animations, Background, and CTA for Testimonials exactly as they were done in the Projects section — same behavior, same integration points.

7. File Naming Conventions
   - Follow the same file naming conventions established in the Projects section to ensure consistency and ease of debugging.

8. Type Definitions
   - Testimonials must have its own dedicated type file.
   - Types may be inherited/reused from other type files where applicable, but the Testimonials-specific type file must still exist and own its domain types.

Deliverable: Assemble all created files into a .zip file and present it.

Please confirm understanding or ask any questions before you proceed.
