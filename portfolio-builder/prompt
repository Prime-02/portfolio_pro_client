Task: Build the Education section of the portfolio, following the same architectural patterns, conventions, and feature set used in the Experience section (core files provided). The Education store file is also provided as a starting point.

Requirements:

1. Card UI Styles & Configurations
   - Education cards must support multiple distinct style variants and configuration options, ensuring the portfolio showcases a mix of design flavors rather than uniform cards.

2. Field-Level Toggle
   - Every individual piece of information within an education entry (e.g., institution, degree, field of study, dates, GPA, description, etc.) must be independently toggleable (show/hide).

3. Layout Types
   - Provide layout types specifically designed for displaying education in a professional standard.
   - These layouts must be visually and structurally distinct from the layouts used in the Experience section.

4. Public Filters
   - Implement filter controls for public users using the following EducationFilters interface exactly:
     export interface EducationFilters {
       skip?: number;
       limit?: number;
       institution?: string;
       degree?: string;
       field_of_study?: string;
       is_current?: boolean;
       ids?: string[];
       merge_filters?: boolean;
     }

5. Card Override Support
   - Education must continue to support card-level overrides, following the same override pattern used in the Experience section.

6. Animations, Background & CTA
   - Implement Animations, Background, and CTA for Education exactly as they were done in the Experience section — same behavior, same integration points.

7. File Naming Conventions
   - Follow the same file naming conventions established in the Experience section to ensure consistency and ease of debugging.

8. Type Definitions
   - Education must have its own dedicated type file.
   - Types may be inherited/reused from other type files where applicable, but the Education-specific type file must still exist and own its domain types.

Deliverable: Assemble all created files into a .zip file and present it.

Please confirm understanding or ask any questions before you proceed.