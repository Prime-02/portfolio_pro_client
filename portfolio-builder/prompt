Task: Build the Blogs section of the portfolio, following the same architectural patterns, conventions, and feature set used in the Projects section (core files provided). The Blogs store file is also provided as a starting point.

Requirements:

1. Card UI Styles & Configurations
   - Blogs cards must support multiple distinct style variants and configuration options, ensuring the portfolio showcases a mix of design flavors rather than uniform cards.

2. Field-Level Toggle
   - Every individual piece of information within an Blogs entry must be independently toggleable (show/hide).

3. Layout Types
   - Provide layout types specifically designed for displaying Blogs in a professional standard.
   - These layouts must be visually and structurally distinct from the layouts used in the Projects section.

4. Public Filters
   - Implement filter controls for public users using the following BlogsFilters interface exactly:
     interface PublicBlogssByUsernameFilters {
     page?: number;
     size?: number;
     is_completed?: boolean;
     is_concept?: boolean;
     Blogs_category?: string;
     Blogs_platform?: string;
     Blogs_status?: string;
     ids?: string[];
     merge_filters?: boolean;
     }

5. Card Override Support
   - Blogs must continue to support card-level overrides, following the simillar override pattern used in the Projects section.

6. Animations, Background & CTA
   - Implement Animations, Background, and CTA for Blogs exactly as they were done in the Projects section — same behavior, same integration points.

7. File Naming Conventions
   - Follow the same file naming conventions established in the Projects section to ensure consistency and ease of debugging.

8. Type Definitions
   - Blogs must have its own dedicated type file.
   - Types may be inherited/reused from other type files where applicable, but the Blogs-specific type file must still exist and own its domain types.

Deliverable: Assemble all created files into a .zip file and present it.

Please confirm understanding or ask any questions before you proceed.
