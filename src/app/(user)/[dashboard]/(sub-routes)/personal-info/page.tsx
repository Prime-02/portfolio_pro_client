"use client";
import { useTheme } from "@/app/components/theme/ThemeContext ";
import React from "react";
const page = () => {
  const { theme } = useTheme();
  return (
    <div>
      <div>
        
      </div>
    </div>
  );
};

export default page;
